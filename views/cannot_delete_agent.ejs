<%- include('partials/header', { title: 'Cannot Delete Agent' }) %>

<div class="max-w-xl mx-auto mt-10 px-4 text-center">
  <h1 class="text-2xl font-bold text-red-600 mb-4">🚫 Cannot Delete Agent</h1>

  <p class="text-gray-700 mb-4">
    The agent <strong><%= agentName %></strong> cannot be deleted because they have
    <strong><%= bookingCount %></strong> existing booking<%= bookingCount === 1 ? '' : 's' %>.
  </p>

  <p class="text-gray-600">
    Please remove their bookings first or disable the agent without deleting them.
  </p>

  <div class="mt-6">
    <a href="/admin/agents" class="text-sm text-blue-600 hover:underline">← Back to Agents</a>
  </div>
</div>

<%- include('partials/footer') %>
